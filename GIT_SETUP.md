**Step to setup git**

git init
git add * 
git commit -m "First commit"
git branch -M main
git remote add origin https://pmarlier:$GITHUB_PERSONAL_TOKEN$@github.com/pmarlier/$REPO_NAME$
actions sur GIT : créer le repo avec le bon nom
git push -u origin main